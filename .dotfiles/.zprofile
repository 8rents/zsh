# =========
# .zprofile
# =========
# Set the environment for the login shell. Basically the same as .zlogin. Where as .zshenv is to set the environment for all instances of zsh (not just where it is the login shell or an interactive shell as with zshrc)
# ---------
# Sourced after .zshenv and before .zshrc
# ---------

