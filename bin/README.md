# User Binaries 

> *This directory holds any user binaary files*

---

Binary files can be installed here. They can be dropped in place or installed as git-submoduels if they have their own repository.

Conversly **aliases** created by the user go into the `aliases` directory.
